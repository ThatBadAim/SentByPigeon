1. **Update `Models.kt`**:
   - Add `SyncState` enum to `Models.kt` (e.g., `PENDING`, `SENT`, `DELIVERED`, `READ`, `FAILED`).
   - Add `syncState: SyncState = SyncState.PENDING` to `Message` data class.

2. **Update `Entities.kt` and `AppDatabase.kt`**:
   - Add `syncState: SyncState` to `MessageEntity`.
   - Update `Converters.kt` with `@TypeConverter` methods for `SyncState`.
   - Update `AppDatabase.kt` version from 1 to 2.
   - Implement an `@AutoMigration(from = 1, to = 2)` inside `AppDatabase.kt`. Note: need to set `exportSchema = true` and ensure schema files exist or set up for it. However, the task says "Room database auto-migrations for version handling". Let's check `AppDatabase.kt` if `exportSchema` is false, change it to true. *Wait*, Android builds require schemas to be exported for `@AutoMigration`. I'll create `schemas` directory if needed, or simply enable `exportSchema = true` and run compilation to generate it. Actually, generating the schema for version 1 first is required for `AutoMigration(from = 1, to = 2)` to work. I will run a script to generate schema v1, or just write a manual `Migration(1, 2)` instead of auto migration if `exportSchema = true` isn't enough to generate v1 schema before updating to v2. Actually, memory says: "Room database schema changes are managed using Room's `@AutoMigration` feature with `exportSchema = true`." and "The application uses WorkManager and Hilt (`androidx.hilt:hilt-work`) for scheduling and managing reliable background tasks."

3. **Dependencies**:
   - Add WorkManager and `hilt-work` dependencies to `libs.versions.toml` and `app/build.gradle.kts`.

4. **Implement `MessageSyncWorker.kt`**:
   - Create `MessageSyncWorker` in `core/work/` extending `CoroutineWorker`.
   - The worker will fetch pending messages (`syncState == SyncState.PENDING`) from `MessageDao`.
   - Attempt to send them via `WebSocketManager` or network call. If successful, update `syncState` to `SENT`. If failed due to no network, retry later.
   - Also update `MessageDao` with methods to fetch pending messages and update message sync state.

5. **Enhance `MessageRepositoryImpl.kt`**:
   - Update `sendTextMessage`, `sendVoiceNote`, `sendMediaMessage` to create entities with `SyncState.PENDING`.
   - Insert entity immediately.
   - Enqueue `MessageSyncWorker` using `WorkManager` (OneTimeWorkRequest with network connectivity constraint) instead of directly calling `webSocketManager.sendFrame` inside `runCatching`? Or try sending immediately and if fails, enqueue worker, or enqueue worker always. The instruction says: "update local Room entities immediately upon send attempts (Optimistic UI updates) before waiting for WebSocket confirmation".
   - So I will insert with `PENDING`, try `webSocketManager.sendFrame`, and on success update DB to `SENT`. If failed, enqueue `MessageSyncWorker`.

6. **Tests**:
   - Run existing tests to verify they are not broken.
   - Run Pre-commit steps.
